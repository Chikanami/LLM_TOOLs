; =============================================
;           数据清洗配置文件
; =============================================
; 说明：
; 1. 每个[节名]对应一个数据集配置
; 2. keep_columns: 需要保留的列名（逗号分隔）
; 3. text_columns：需要保证是文本的列名（逗号分隔）（用于各类数字id）
; 3. double_header: 是否跳过第一行表头（true/false）
; 4. merge_same_base: 是否合并相同数据集（true/false）
; 5. file_pattern: 文件匹配模式（支持*通配符）

[有效商户数据]
; 保留以下列（确保列名完全匹配）
keep_columns =

; 需要保证是文本的列名（用于各类数字id）
text_columns =

; 有没有双行表头
double_header = true

; 合并所有文件
merge_same_base = true

; 匹配格式：XXXX_yyyy_mm_dd.xlsx 等
file_pattern = 